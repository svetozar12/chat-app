{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "kill": "npx kill-port 3000 4000 4001 4002 -y",
    "dev": "yarn kill && lerna run dev --no-sort --stream",
    "dev:backend": "yarn kill && lerna run dev:backend --no-sort --stream",
    "test": "lerna run test --no-sort --stream",
    "lint": "lerna run lint --no-sort --stream",
    "lint-css": "lerna run lint-css --no-sort --stream",
    "diff": "lerna diff",
    "format": "lerna run format --no-sort --stream",
    "dev:kill": "yarn kill && yarn dev",
    "mash-up": "yarn format && yarn lint && yarn build",
    "mash-up:test": "yarn format && yarn lint && yarn build && yarn test"
  },
  "devDependencies": {
    "lerna": "^5.1.8",
    "nx": "^14.4.3"
  },
  "engines": {
    "npm": "please-use-yarn",
    "yarn": ">= 1.17.3",
    "node": ">= 12.5.0"
  },
  "name": "chat-app"
}
